# Uncomment this line to define a global platform for your project
platform :ios, '9.0'

target 'aw' do
  use_frameworks!

  # Pods for aw
  pod 'ActiveLabel', '~> 0.8'
  pod 'Fabric'
  pod 'Crashlytics'
  
end
